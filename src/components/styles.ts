import styled from 'styled-components';

export const MainContainer = styled.div`
    background: linear-gradient(to right, var(--color-bg) 62.25%, var(--color-bg-header) 0%);
    .container-default {
        max-width: 1320px;
        margin: 0 auto;
    }
    
    .bg-default {
        background: var(--color-bg);
    }
`;